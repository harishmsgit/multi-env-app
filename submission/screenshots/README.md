# Screenshots

Add the following screenshots here after validation:

- `pods.png`: output of `kubectl get pods`
- `logs.png`: service communication logs or pod logs
- `service-test.png`: results from local gateway curl tests or port-forward validation
